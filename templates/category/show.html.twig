{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>NameCategory</th>
                <td>{{ category.nameCategory }}</td>
            </tr>
            <tr>
                <th>PictureCategory</th>
                <td>{{ category.pictureCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">back to list</a>

{% endblock %}
