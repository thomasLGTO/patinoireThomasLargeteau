{% extends 'base.html.twig' %}

{% block title %}{{name}}{% endblock %}

{% block body %}

    <div class="container">
        <h2>Formulaire de modification du tips</h2>
        {% if is_granted('ROLE_ADMIN') or (app.user and tip.status == 'refusé') or (app.user and tip.status == 'en attente') %}
            {{ include('tips/_form.html.twig', {'button_label': 'Enregister'}) }}
        {% else %}
            <p >Vous ne pouvez pas modifier ce tips </p>
        {% endif %}
        {% if is_granted('ROLE_ADMIN') %}
            {{ include('tips/_delete_form.html.twig') }}
        {% endif %}
    </div>
{% endblock %}
