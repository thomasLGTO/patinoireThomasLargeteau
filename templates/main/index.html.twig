{% extends 'base.html.twig' %}

{% block title %}{{name}}{% endblock %}

{% block body %}
    <div class="container">
    <h2 class="my-5">Les derniers Tips</h2>
        <div class="row justify-content-between no-gutters">
            <div class="boxAddTips d-flex justify-content-between col-8 mb-5">
                {% if not app.user %}
                    <a href="{{ path('app_login') }}" class="btn btn-primary px-5">Ajouter un tips</a>
                {% else %}
                    <a href="{{ path('tips_new') }}" class="btn btn-primary px-5">Ajouter un tips</a>
                {% endif %}
            </div>
            {% include("main/searchBar.html.twig") %}
            {{ include("tips/boxTips.html.twig",{sort : pagination}) }}
            {{ render(controller(
                'App\\Controller\\CategoryController::listCategory'
            )) }}
            <div class="rounded col-lg-8 mt-5 presentation" {## style="background-image: linear-gradient(rgba(52, 73, 94, 0.5), rgba(52, 73, 94, 0.5)), url({{ asset('images/path.jpg') }});"#}>
                <div class="container h-100">
                    <div class="row align-items-center h-100 justify-content-center ">
                        <img src="{{ asset('images/logo.png') }}" width="80" height="80"  alt="Logo du site Tips">
                        <p class="text-white textPresentation col-10  ">
                            Un tips est une astuce simple et efficace pour résoudre un problème récurrent. Vous savez cette idée toute bête, toute simple et qui pourtant vous facilite tellement la vie ! Les Tips nous permettent de gagner du temps, d’améliorer notre comportement face aux difficultés répétitives. Souvent au premier abord un tips paraît être une idée simple et évidente, pourtant lorsque nous la connaissant pas on ne la trouve pas forcément. Ne cherchez plus, les tips que vous avez besoin sont ici, et si vous ne trouvez pas ce que vous cherchez, créez votre propre tips à partager.
                        </p>
                    </div>
                </div>
            </div>
            <div class="col-8 col-lg-3 m-auto ctaRegister mt-5  ">
                <a href="{{ path('app_register') }}"><img class="w-100"src="{{ asset('images/ctaRegister.png') }}" alt="Photo d'un groupe d'individu"></a>
            </div>
        </div>
    </div>
{% endblock %}
